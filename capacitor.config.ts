import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cohbrgr.app',
  appName: 'cohbrgr',
  webDir: 'apps/shell/dist/client'
};

export default config;
